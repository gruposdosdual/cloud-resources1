terraform {
  backend "s3" {
    bucket         = "mi-terraform-tfstate"
    key            = "terraform/state/terraform.tfstate"
    region         = "eu-west-1"
    encrypt        = true
    #dynamodb_table = "terraform-lock"
  }
}
